SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
# Same-origin in production under Vercel Services, so this mostly matters
# for local dev when running this backend as its own process (not via
# `vercel dev`) alongside `npm run dev` in frontend/.
ALLOWED_ORIGINS=http://localhost:3000
